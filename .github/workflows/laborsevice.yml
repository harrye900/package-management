name: laborservice

on:
  pull_request:
  push:

jobs:
  laborservice:
    runs-on: ubuntu-latest

    steps:
      - name: echo string
        run: echo "hello"

      - name: Multiple line string
        run: |
          pwd
          node -v
          ls -a
      - name: python command
        run: |
          import platform
          print(platform.processor())
        shell: python

    
